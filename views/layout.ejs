<!DOCTYPE html>
<html>
<head>
    <title>Quiz</title>
    <link rel='stylesheet' type="text/css" href='/stylesheets/style.css' />
    <link rel='stylesheet' type="text/css" href='/stylesheets/wide.css'
          media="only screen  and (min-width: 530px) and (min-device-width: 481px)" />
    <link rel='stylesheet' type="text/css" href='/stylesheets/smartphones.css'
          media="only screen and (max-width: 480px)"/>

</head>
<body>
    <div id="page-wrap">
        <header class="main" id="h1">
            <% if(!session.user){ %>
                <span class="right"><a href="/login">Login</a></span>
            <% } else { %>
                <span class="right"><%= session.user.username %>  <a href="/logout">Logout</a></span>
            <% } %>
            <h2>Quiz <span>El juego de las preguntas</span></h2>
        </header>
        <nav class="main" id="n1" role="navigation">
            <span><a href="/">Inicio</a></span>
            <span><a href="/quizes">Preguntas</a></span>
            <span><a href="/author">Acerca del Autor</a></span>
        </nav>
        <section class="main" id="s1">
            <% if(errors.length){ %>
                <span id="ErrorMsgs">
                    Corregir Errores
                    <% for(var i in errors){ %>
                        <span> <%= errors[i].message %></span><br/>
                    <% } %>
                </span>
            <% }%>
            <%- body %>
        </section>
        <footer class="main" id="f1">
            <a href="https://github.com/yailPeralta/quiz">Quiz en GitHub</a>
        </footer>
    </div>
</body>
</html>